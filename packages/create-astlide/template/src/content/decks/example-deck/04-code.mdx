---
slideLayout: code
notes: "The <CodeBlock> component adds a filename header and a copy button to any code block."
---

# CodeBlock Component

<CodeBlock title="src/content.config.ts">
```typescript
import { defineCollection } from 'astro:content';
import { glob } from 'astro/loaders';
import { slideSchema } from '@astlide/core/schema';

const decks = defineCollection({
  loader: glob({ pattern: '**/*.mdx', base: 'src/content/decks' }),
  schema: slideSchema,
});

export const collections = { decks };
```
</CodeBlock>

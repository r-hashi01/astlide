---
slideLayout: default
notes: "This whole slide is authored in plain Markdown (.md) — no MDX required."
---

# Markdown slides

This slide is a plain **`.md`** file — no MDX, no import lines.

- Frontmatter works the same (`slideLayout`, `background`, `notes`, …)
- Great for quick, prose-heavy slides
- Rendered by Astro's own Markdown pipeline

> Mix `.md`, `.mdx`, and `.html` freely in one deck.
